apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo.api.first.fullname" . }}-test-connection"
  labels:
    {{- include "demo.api.first.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo.api.first.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
